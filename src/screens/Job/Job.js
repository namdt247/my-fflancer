import React from 'react';
import PropTypes from 'prop-types';
import MainLayout from '../../layout/MainLayout';

function Job(props) {
  return (
    <MainLayout>Job page</MainLayout>
  );
}

Job.propTypes = {

};

export default Job;
